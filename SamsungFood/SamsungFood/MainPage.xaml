<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SamsungFood.ViewModels"
             x:Class="SamsungFood.MainPage"
             BackgroundColor="Orange">

    <ContentPage.BindingContext>
        <vm:MainViewModel />
    </ContentPage.BindingContext>

    <Grid>
        <!-- Main Content -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackLayout Grid.Row="0" Padding="10" BackgroundColor="White">
            <Label Text="Your Tailored Plan" 
                   FontSize="24" 
                   FontAttributes="Bold"
                   HorizontalOptions="Start" TextColor="Gray" Margin="10,0,20,0"/>
            <Label Text="These are your selected meals based on your preferences."
                   FontSize="14"
                   TextColor="Gray"
                   HorizontalOptions="Start" Margin="20" />
            <Button Text="Add a Plan"
                    BackgroundColor="#FFA500"
                    TextColor="White"
                    CornerRadius="20" 
                    HorizontalOptions="Center"
                    WidthRequest="120"
                    HeightRequest="40" />
        </StackLayout>

        <!-- Meal Plan List -->
        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding MealPlans}"
                        Margin="10">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="40" Margin="5" BackgroundColor="#F8F8F8" BorderColor="Transparent" CornerRadius="15">
                        <Grid ColumnDefinitions="2*,3*" RowDefinitions="Auto,Auto">
                            <Image Source="{Binding Image}" 
                                   Aspect="AspectFill" 
                                   HeightRequest="100" 
                                   WidthRequest="100"
                                   VerticalOptions="Center" />
                            <StackLayout Grid.Column="1" Padding="10">
                                <Label Text="{Binding Title}" 
                                       FontSize="16" 
                                       FontAttributes="Bold" TextColor="Black" />
                                <Label Text="{Binding Date}" 
                                       FontSize="14" 
                                       TextColor="Gray" />
                            </StackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Bottom Navigation Bar -->
        <StackLayout Grid.Row="2" Orientation="Horizontal" BackgroundColor="#F0F0F0" HeightRequest="60">
            <Button BackgroundColor="Transparent" Text="Home" TextColor="Black" HorizontalOptions="FillAndExpand" />
            <Button BackgroundColor="Transparent" Text="Explore" TextColor="Black" HorizontalOptions="FillAndExpand" />
            <Button BackgroundColor="Transparent" Text="My Plan" TextColor="Black" HorizontalOptions="FillAndExpand" />
            <Button BackgroundColor="Transparent" Text="Profile" TextColor="Black" HorizontalOptions="FillAndExpand" />
        </StackLayout>
    </Grid>

</ContentPage>
